{
  "name": "puppeteer-mocha",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx mocha --timeout=30000 --require mocha-steps ./build/tests/**.js",
    "clean": "rm -rf build",
    "build": "babel --preset es2015 -d build/ src/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chai": "^5.1.2",
    "mocha": "^11.1.0",
    "mocha-steps": "^1.3.0",
    "puppeteer": "^24.1.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
